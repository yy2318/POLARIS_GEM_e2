#!/usr/bin/env python3
import argparse
p=argparse.ArgumentParser();p.add_argument('bag');p.add_argument('output');a=p.parse_args();raise SystemExit('Configure actual state/control topic names, then implement rosbag extraction for your simulator interface.')
